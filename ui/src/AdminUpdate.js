import React from "react";

export default function AdminUpdate() {
  return (
    <div style={{padding:20}}>
      <h3>Admin (placeholder)</h3>
      <p>Use the server API endpoints to list updates/backups once the server is running.</p>
    </div>
  );
}
